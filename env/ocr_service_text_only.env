# IMPORTANT: base env (ocr_service.env) is loaded first; this file should only override what differs.

# text-only mode
OCR_SERVICE_OPERATION_MODE=NO_OCR

# optional: change port if running alongside the OCR service
OCR_SERVICE_EXTERNAL_PORT=8091
# keep internal port unless you change the service binding
OCR_SERVICE_INTERNAL_PORT=8090

# optional: override worker/thread counts for text-only profile
#OCR_WEB_SERVICE_WORKERS=1
#OCR_WEB_SERVICE_THREADS=1

# optional: override shm size if text-only needs different memory
# OCR_SERVICE_DOCKER_SHM_SIZE=512m
